// EC2 Connection Test - Multiple IP options
// This script tests different connection methods for your EC2 instance

// Configuration - UPDATE THESE WITH YOUR ACTUAL IPs
mode := interpret;
target := ["testuser@localhost:2222"];
setting := "configuration";
super_user := true;
on_error := "rollback";

// Try your EC2's PUBLIC IP here (get from AWS console)
// Replace with your actual public IP: ec2-xx-xx-xx-xx.compute-1.amazonaws.com
target := "ec2-user@YOUR_PUBLIC_IP_HERE";

fn main() {
    print("🔍 EC2 Connection Diagnostics");
    print("=============================");
    print("Target: " + target);
    print("");
    
    print("⚠️  IMPORTANT: Update the target IP in this script!");
    print("   1. Go to AWS Console → EC2 → Instances");
    print("   2. Find your instance");
    print("   3. Copy the PUBLIC IPv4 address or Public DNS");
    print("   4. Update the 'target' variable above");
    print("");
    
    // Basic connection test
    connection_test();
    
    print("✅ Connection test completed");
}

fn connection_test() {
    print("🧪 Testing basic connection...");
    
    // Simple system info test
    sysinfo();
    
    print("✅ If you see system info above, the connection works!");
}